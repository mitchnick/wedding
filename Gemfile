source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.3'

# Use pg as the database for Active Record
gem 'pg'

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

gem "haml-rails"   # Similar to ERB but allows you to write less code in your views
gem 'font-awesome-rails'  # Icons that can be used easily in the application

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

# For my simple contact us form
gem 'active_attr'

# For deploying
# gem 'capistrano'
# gem 'capistrano-rails'
# gem 'capistrano-rvm'
# gem 'capistrano-bundler'
# gem 'activerecord-postgresql-adapter'
# gem 'figaro' # Manages environment variables

# For invitations acceptance
gem 'devise'
gem 'jquery-datatables-rails', '~> 3.1.1'
gem 'simple_form'
gem 'annotate'
gem "select2-rails"

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

group :production do
  gem 'rails_12factor'                    # Needed for heroku
end

group :development do
  gem "letter_opener", '~> 1.2'    # Preview mail in the browser instead of sending
  gem 'quiet_assets'               # Show a LOT less in the rails s logger
  gem 'byebug'                     # Debugger that works with Ruby 2.0
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.1.2'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
